# Hostinger añade encima el bloque Passenger (NO lo borres en File Manager).
# Solo archivos estáticos reales; /onboarding lo atiende Node (SERVE_SPA).

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  RewriteCond %{REQUEST_FILENAME} -f
  RewriteRule ^ - [L]
</IfModule>
