# MySQL — Hostinger (copia a .env y rellena la contraseña)
DATABASE_HOST=srv1855.hstgr.io
DATABASE_PORT=3306
DATABASE_NAME=u301973293_correctly
DATABASE_USER=u301973293_admin
DATABASE_PASSWORD=
# En Hostinger: preferir Base64 (el panel corta &)
# echo -n 'tu-password' | base64
DATABASE_PASSWORD_B64=

# Prisma (se puede omitir si USER+PASSWORD+HOST+NAME están)
DATABASE_URL=

# App
PORT=3000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# En Hostinger (Opción A — ver HOSTINGER.md):
# - NO definir PORT
# - NO definir SERVE_SPA (Apache sirve el front)
# - NODE_ENV=production
# - CLIENT_URL=https://jeisson.click
# - DATABASE_PASSWORD_B64=...
# - ALLOW_DEMO_LOGIN=true

# Google OAuth (rellenar al configurar)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# JWT
JWT_SECRET=change-me-to-a-long-random-string

# Superadmins por email exacto (además de demo@correctly.app y jeissondav1@gmail.com)
# SUPERADMIN_EMAIL=otro@ejemplo.com
# SUPERADMIN_EMAILS=a@ejemplo.com,b@ejemplo.com
