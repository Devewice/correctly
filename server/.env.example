# MySQL — Hostinger (copia a .env y rellena la contraseña)
DATABASE_HOST=srv1855.hstgr.io
DATABASE_PORT=3306
DATABASE_NAME=u301973293_correctly
DATABASE_USER=u301973293_admin
DATABASE_PASSWORD=

# Prisma (contraseña URL-encoded: : → %3A  ? → %3F  > → %3E)
DATABASE_URL="mysql://u301973293_admin:y%3AZz3L%3F%3Ep6@srv1855.hstgr.io:3306/u301973293_correctly"

# App
PORT=3000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# En Hostinger (panel):
# - NO definir PORT (Hostinger lo inyecta; si pones 3000 → 404)
# - NODE_ENV=production
# - CLIENT_URL=https://jeisson.click
# - DATABASE_URL con password encoded (no la palabra PASSWORD)
# - GOOGLE_CALLBACK_URL=https://jeisson.click/api/auth/google/callback  (sin :3000)

# Google OAuth (rellenar al configurar)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# JWT
JWT_SECRET=change-me-to-a-long-random-string
